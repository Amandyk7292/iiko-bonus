const fs = require('node:fs');
const path = require('node:path');

const root = path.resolve(__dirname, '..');
const source = fs
  .readFileSync(path.join(root, 'src/data/customer-catalog-translations.txt'), 'utf8')
  .trim();
fs.writeFileSync(
  path.join(root, 'BulkaAndroid/lib/core/catalog_copy.g.dart'),
  `// Generated by scripts/generate-catalog-copy.js. Edit the shared catalog text file.\npart of '../main.dart';\n\nconst _catalogCopyRows = r'''\n${source}\n''';\n`,
);
